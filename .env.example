# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# Mock admin credentials (dev-only — used ONLY when Supabase is not configured)
# Do NOT set these in production. Remove them when deploying.
VITE_MOCK_ADMIN_EMAIL=admin@yoursite.com
VITE_MOCK_ADMIN_PASSWORD=change-me-in-dev
